// Expo Router Configuration
// Explicitly specify the app directory to prevent confusion with src/app (Next.js)
module.exports = {
  // Force Expo Router to use the root 'app' directory, not 'src/app'
  appDir: 'app',
};
