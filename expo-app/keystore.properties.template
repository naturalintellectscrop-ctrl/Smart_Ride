# ============================================================
# Smart Ride — Keystore Password (LOCAL ONLY, GITIGNORED)
# ============================================================
# Create this file at: C:\Smart_Ride\expo-app\keystore.properties
# with your keystore password. The build script reads it automatically
# so you don't have to type the password every time.
#
# The keystore is: keystores/smartride-upload.keystore
# The alias is:    smartride
#
# Replace YOUR_PASSWORD_HERE with the actual keystore password.
# ============================================================

KEYSTORE_PASSWORD=YOUR_PASSWORD_HERE
