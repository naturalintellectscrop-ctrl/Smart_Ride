// The shared storefront kit — one visual language for every surface where
// somebody runs a shop on Smart Ride: the pharmacy, the restaurant, the
// grocer. Colour says what a number MEANS (amber waiting, blue in flight,
// green done, violet needs a prescription), and money always sits on the one
// dark green field so it is never read as an ordinary statistic.
export * from './storefrontKit';
export * from './pharmacyOrder';
export * from './merchantOrder';
